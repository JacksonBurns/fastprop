def main():
    # define a dictionary that contains the default settings for fastprop

    # load the input yaml file into a dictionary (can either be train or predict) or parse the command line arguments
    # and convert them to a dictionary with vars(args)
    # and then call dict.update() to set the new parameters

    # then call fastprop